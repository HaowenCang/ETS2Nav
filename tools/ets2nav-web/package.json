{
  "name": "ets2nav-web",
  "version": "0.1.0",
  "private": true,
  "description": "ETS2Nav 正式 Web UI（§56-61）——离线/局域网可运行，依赖经 npm 锁定后构建为静态产物",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs"
  },
  "dependencies": {
    "maplibre-gl": "4.7.1",
    "pmtiles": "4.5.0",
    "qrcode": "1.5.4"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  },
  "license": "GPL-3.0-only"
}
