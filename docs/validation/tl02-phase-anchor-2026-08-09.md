# TL-02 Phase Anchor 实验结果（2026-08-09）

状态：⏳ **未决**——跨会话 sim 比较无效（两会话间游戏重启+快速旅行，simulation_time 已重置），需补做「sim 连续窗口」对照实验

## 已确认的事实

1. 两次会话的段长模式完全一致（绿≈23.9/黄≈3.0/红≈32.0/红黄≈0.9s）——**重启+快速旅行后信号灯仍按相同 profile 运行**（TL-04 初步数据）。
2. 会话内周期稳定（59.55s / 59.8–60.1s），倍率恒为 1.000（TL-01 再确认）。
3. 跨会话 R2G 相位（sim mod 周期）不同（0.8 vs 5.9s）——但因 sim 重置**不能**作为 H2 证据。

## 未决问题

H1（全局时钟）vs H2（加载锚定）需要「同一游戏会话内（sim 连续）、灯组卸载重载前后」的相位对照：

| 实验 | 操作 | 判定 |
|---|---|---|
| TL-02a 方法验证 | 路口 A 记录会话 1 → 原地等 1 分钟 → 记录会话 2 | 相位连续（方法自检） |
| TL-02b 卸载重载 | 驶离 2km+ 返回同一路口 → 记录会话 3 | 相位跳变 → H2；相位延续 → H1 |
| TL-04 快速旅行 | 快速旅行后立即返回 → 记录会话 4 | 相位重置观察 |

## 对 P0-B 的意义（H2 若确认）

1. 纯计算倒计时不可行；需观测锚定（灯组可见时锚定一次相位，用 interval+simulation_time 外推）。
2. interval 秒=真实秒 → 外推无需倍率换算（TL-01）。
