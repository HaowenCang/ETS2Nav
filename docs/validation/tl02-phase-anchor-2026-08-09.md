# TL-02 Phase Anchor 实验结果（2026-08-09）

状态：🔄 初步结论指向 H2（加载锚定），**待确认两会话间操作**（见文末）

## 实验设置

同一路口两次独立会话（灯组在会话间必然经历卸载/重载）：
- 会话 A（21:14:11）：3 个转换，2 个 R2G
- 会话 B（21:22:41）：12 个转换，3 个 R2G（3 个完整周期）

## 会话内数据（周期稳定性）

| 会话 | R2G 的 simulation_time (s) | 会话内周期 |
|---|---|---|
| A | 240.79、300.34 | 59.55 s |
| B | 726.04、785.84、845.93 | 59.8 / 60.1 s |

会话内周期稳定（±0.5%），与 TL-01 的 59.5s 一致。B 会话的 3 个周期段长重复精确（绿 23.9/黄 3.0/红 32.0/红黄 0.9）。

## 跨会话判定

- A 会话 R2G 相位（sim mod 周期）≈ 0.8s
- B 会话 R2G 相位 ≈ 5.9s
- A 末 R2G → B 首 R2G = 425.7s = 7.095 个 60s 周期（**余 5.7s，非整数周期**）

**若两次会话之间 simulation_time 连续**（纯驾驶往返，无读档/快速旅行/传送），则相位在灯组重载后**不连续** → **H2（局部加载锚定）确认**：灯组每次加载从默认状态重新开始计时，相位与全局时钟无关。

**前提**：跨会话 sim 比较仅在 simulation_time 未重置时有效（官方 timer_restart flag 表明读档/快速旅行可能重置 sim）。

## 待确认

❓ 用户操作史：会话 A 与 B 之间（21:14–21:22）是否发生过：
- [ ] 仅驾驶往返（sim 连续）→ H2 可直接确认
- [ ] 快速旅行 / 读档 / 传送 / 重启游戏 → 需重新设计对照（在 sim 连续窗口内做"驶离→返回"实验）

## 对 P0-B 的意义（若 H2 确认）

1. **纯计算倒计时不可行**：任意路口的相位是加载时刻的函数，无法从游戏时钟先验推导。
2. **观测锚定方案可行**：灯组可见时观测一次相位（视觉识别），用 interval（真实秒）+ simulation_time 外推后续转换。锚定后误差 = 周期累积误差（需评估 interval 精度）。
3. 与 TL-01 结合：interval 秒 = 真实秒 → 外推计算无需倍率换算。
